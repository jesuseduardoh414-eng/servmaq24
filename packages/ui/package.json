{
  "name": "@servmaq/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Componentes base del design system. REGLA: consumen únicamente variables CSS de tokens (var(--...)), jamás valores literales.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.8.0"
  }
}
