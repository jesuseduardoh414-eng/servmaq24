# Copia este archivo a .env (nunca versionar .env — lección del proyecto anterior)
# Dev local: MariaDB de XAMPP, BD maqserv_dev (root sin contraseña es el default de XAMPP)
DATABASE_URL="mysql://root:@localhost:3306/maqserv_dev"
