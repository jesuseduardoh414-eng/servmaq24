{
  "name": "@servmaq/types",
  "version": "0.1.0",
  "private": true,
  "description": "DTOs compartidos front↔back. La API responde exactamente estas formas; el front consume exactamente estas formas.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
